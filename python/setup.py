from setuptools import setup

setup(
    name = 'taskrelay',
    version = '0.1.0',
    description = 'A library to run tasks on remote servers',
    license = 'MIT',
    url = 'https://github.com/muntashir/taskrelay',
    author = 'https://github.com/muntashir',
    packages = [''],
    install_requires = ['websockets']
)
